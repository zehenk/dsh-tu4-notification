# v0.5.0 测试报告

日期：2026-09-15 ｜ 环境：Windows（web profile，GUI 127.0.0.1:3080）｜ 结论：**✅ 全部通过，已部署**

## 验证方式

真实审批触发：agent 对工作区外路径（`C:\Users\zehenk\`）发起 `write` 并请求
`danger-full-access` 提权 → DSH 派发 `approval/request` → 插件弹卡片。

## 生产验证（4/4 通过）

| # | 操作 | 结果 | 结论 |
|---|---|---|---|
| A | 卡片点「允许一次」 | 文件成功写入（`dsh-v05-verify-A2.txt`），内容正确 | ✅ claim `allowed-once`，与 GUI 按钮同效果 |
| B | 卡片点「拒绝」 | 文件未写入，工具调用被拒 | ✅ claim `rejected`，与 GUI 按钮同效果 |
| C | 不碰卡片 | 卡片 ≈18s 淡出 → Web GUI 出现 pending 条目 → GUI 应答（拒绝）生效 | ✅ claim 窗口到期 → next() 委托宿主 |
| D | 点右上角 × | 卡片立即关闭（不应答）→ GUI 条目出现 → GUI「允许一次」→ 文件写入 | ✅ × 仅关卡片，GUI 回退正常 |

**核心验收达成**：卡片「拒绝」/「允许一次」按钮产生的 outcome 与 DSH GUI
同名按钮完全一致（`rejected` / `allowed-once`）。

## 自动化验证（冒烟测试 10/10 通过）

1. 模块契约（name + apply）
2. notify.ps1 纯 ASCII
3. listener 注册（approval/request + prepend）
4. 卡片应答 allowed-once → claim（next 未调用）
5. 卡片应答 rejected → claim（next 未调用）
6. claim 窗口到期 → next() 委托 + 结果透传
7. 卡片启动失败 → 立即 next() + msg 回退（音效+弹窗各一次）
8. 非 Windows / 已中止 → 直接 next()
8b. claim 期间 abort → settle cancelled（next 未调用）
9. 集成端到端：listener → next() 委托 + 宿主结果透传

## 其他验证

- PS 5.1 语法解析：`Parser::ParseFile` 0 错误
- 预览渲染：`-PreviewOut` 中英文双版本 PNG 布局正确（428×124 设计单位）
- IPC 往返：PS `Set-Content`（含 CRLF）→ Node 读取 + trim → 词汇匹配 → 文件清理
- 生产环境 IPC 残留检查：验证完成后 `%TEMP%\dsh-tu4-notify-*.txt` 计数 = 0

## 观察与备注

- 卡片按钮点击后卡片立即消失（claim 即时生效），无需等待
- GUI 回退条目出现时机 ≈ 卡片出现后 18s（claim 窗口 = 15s + 3s 余量），与设计一致
- 无残留 pending 条目、无 GUI 闪现、无 IPC 文件泄漏
